"""
Infrastructure layer for the ``core`` context.

Concrete implementations of domain ports and other integrations live here.
This layer may depend on external packages (databases, messaging, HTTP,
etc.) but should not depend on the application layer.
"""