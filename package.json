{
  "name": "HomeStation_Core",
  "version": "0.1.0",
  "private": true,
  "description": "Generated Nx monorepo with hexagonal architecture",
  "scripts": {
    "build": "nx run-many --target=build",
    "lint": "nx run-many --target=lint",
    "test": "nx run-many --target=test",
    "typecheck": "nx run-many --target=typecheck"
  },
  "devDependencies": {
    "nx": "^17.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "apps/*",
    "libs/*",
    "tools"
  ]
}
